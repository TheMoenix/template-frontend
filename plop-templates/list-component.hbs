import React from 'react';
import { List, Card } from 'antd';

interface {{properCase name}}ListProps {
  items?: any[];
  loading?: boolean;
}

export const {{properCase name}}List: React.FC<{{properCase name}}ListProps> = ({ 
  items = [], 
  loading = false 
}) => {
  return (
    <List
      grid={{ gutter: 16, xs: 1, sm: 2, md: 3, lg: 3, xl: 4, xxl: 4 }}
      dataSource={items}
      loading={loading}
      renderItem={(item) => (
        <List.Item>
          <Card title={item.name || item.title}>
            <p>Item content goes here</p>
          </Card>
        </List.Item>
      )}
    />
  );
};
